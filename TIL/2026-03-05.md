# 2026-03-05 TIL

---

## Wargames

- **Another Ping**
  - Command Injection
  - 오류 메시지 해석 실패, 공백 문자 우회는 $IFS

- **Gyul_Console**
  - eval(): 문자열을 받아서 파이썬 코드로 실행하는 매우 취약한 함수
  - \_\_import\_\_('os').popen('cat ../flag.txt').read()

- **Tap Tap!**
  - SSRF
  - 내부망에 대한 이해
  - docker-compose.yml에서 힌트 얻기
  - 서버를 이용해 내부망에 요청 보내기

- **Farewell**
  - data={}는 body에, params는 쿼리에
  - import string
  - session = requests.Session() -> session.get() 으로 속도 향상

- **Merry Christmas**
  - 블랙박스 세션 쿠키 변조
  - 클라이언트 사이드 세션: Flask는 서버 메모리를 아끼려고 데이터를 클라이언트한테 다 줘버린다. 대신 위조를 막기 위해 **비밀번호(Secret Key)로 만든 암호화된 도장(서명)**을 쿠키 맨 끝에 붙여놓는 방식을 쓴다
  - 암호화 알고리즘마다 만들어내는 서명의 '고정된 길이'가 다르다! (Base64로 인코딩했을 때 기준)
  - 27글자: 무조건 SHA1(옛날 버전 Flask의 기본값)
  - 43글자: 무조건 SHA256(중간 버전)
  - 86글자: 무조건 SHA512(요즘 최신 버전 Flask의 기본값)
